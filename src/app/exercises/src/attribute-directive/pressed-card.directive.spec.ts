import { PressedCardDirective } from './pressed-card.directive';

describe('PressedCardDirective', () => {
  it('should create an instance', () => {
    const directive = new PressedCardDirective();
    expect(directive).toBeTruthy();
  });
});
